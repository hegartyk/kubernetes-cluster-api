package schema

const (
	// ApplicationPrefix is the prefix for all kubernetes cluster nodes
	ApplicationPrefix = "kube"
	// ClusterPrefix is the prefix for all Cluster nodes
	ClusterPrefix = ApplicationPrefix + "cls"
)
